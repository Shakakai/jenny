# Anthropic API key — set this before running the app
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Tauri dev mode — set automatically by `make dev`, do not set manually
# TAURI_DEBUG=1
