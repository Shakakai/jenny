[package]
name = "agentis"
version = "0.1.0"
description = "Agentis — Mesa agent-based model builder"
authors = ["Agentis"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-store = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.11", features = ["blocking"] }

[profile.dev]
incremental = true

[profile.release]
panic = "abort"
codegen-units = 1
